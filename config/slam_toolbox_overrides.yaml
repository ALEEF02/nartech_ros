slam_toolbox:
  ros__parameters:
    # Keep SLAM on the fresh lidar stream; /scan remains muxed for Nav2.
    scan_topic: /scan/livox
    base_frame: base_scan_level
    odom_frame: odom
    transform_publish_period: 0.02

/slam_toolbox:
  ros__parameters:
    scan_topic: /scan/livox
    base_frame: base_scan_level
    odom_frame: odom
    transform_publish_period: 0.02

sync_slam_toolbox_node:
  ros__parameters:
    scan_topic: /scan/livox
    base_frame: base_scan_level
    odom_frame: odom
    transform_publish_period: 0.02

/sync_slam_toolbox_node:
  ros__parameters:
    scan_topic: /scan/livox
    base_frame: base_scan_level
    odom_frame: odom
    transform_publish_period: 0.02
